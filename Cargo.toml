[workspace]
resolver = "2"
members = [
    "crates/infekt",
    "crates/infekt-core",
    "crates/infekt-cli",
]

[profile.release]
strip = true
lto = "fat"

# The profile that dist uses for shippable binaries.
[profile.dist]
inherits = "release"
